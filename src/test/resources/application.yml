spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        # ✅ dialect 설정을 이 위치로 옮겨야 합니다.
        dialect: org.hibernate.dialect.H2Dialect